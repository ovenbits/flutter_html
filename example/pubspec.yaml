name: example
description: flutter_html example app.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter_html:
    path: ..
  flutter_html_all:
    path: ../packages/flutter_html_all
  flutter:
    sdk: flutter
  wakelock_web: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/html5.png
    - assets/mac.svg
